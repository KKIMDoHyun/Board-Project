import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: '100%',
    backgroundColor: '#e2f5e1',
    alignItems: 'center',
  },
  contentContainer: {
    width: 450,
    marginTop: 15,
  },
});
