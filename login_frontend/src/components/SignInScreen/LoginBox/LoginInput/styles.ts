import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    backgroundColor: 'green',
  },
  textContainer: {
    width: '100%',
    height: 50,
    borderWidth: 1,
    padding: 10,
  },
  mb10: {
    marginBottom: 10,
  },
});
